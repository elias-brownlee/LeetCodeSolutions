class Solution {
public:
    int reverse(int x) {
        long y = 0;
        while(x){
            int z = x % 10;
            y *= 10;
            y += z;
            x /= 10;
        }
        if (y > INT_MAX || y < INT_MIN){
            return 0;
        }
        return int(y);
    }
};
